# 🇨🇵 RDVS Numériques DuMAS - 5 mars 2026<br>
# "Deep Learning: a simple Dense Neural Network to classify the MNIST images and a Convolutional Neural Network to classify micrographs."

Ce dépôt contient les documents de la deuxième partie de la journée des "RDVS Numériques DuALS" du 5 juin 2026 à l'ENSAM de Talence, consacrée au Deep Laerning.<br>

Pour utiliser les notebooks (Jupyter ou marimo):
- Installe le gestionnaire de projets Python __uv__ (voir https://docs.astral.sh/uv/getting-started/installation/)
- Telécharge le zip du dépôt GitHub (bouton vert [Code V] en haut de la page)
- Dézippe l'archive et dans une console (*terminal* Linux / MacOs ou *PowerShell* Windows) va dans le dossier `Micrographs_classif` en tapant la commande : 
`cd <path to>/Micrographs_classification-master>`<br>
où `<path to>/Micrographs_classification-master` est le chemin d'accès au dossier `Micrographs_classification-master` que tu peux récupérer avec le navigateur de fichiers de ta machine.
- Tape la commande `uv sync` pour installer les modules Python dans l'Environnement Virtuel Python du dossier `Micrographs_classification-master`.
- Lance jupyter en tapant `uv run jupyter lab` ou marimo en tapant `uv run marimo edit`.

# 🇬🇧 RDVS Numériques DuMAS - 5 mars 2026<br>
# "Deap Learning: a simple Dense Neural Network to classify the MNIST images and a Convolutional Neural Network to classify micrographs."
This repository contains the documents from the second part of the "RDVS Numériques DuMAS" day on June 5, 2026, at ENSAM in Talence, devoted to Deep Learning.<br>

To use the notebooks (Jupyter or marimo):
- Install the Python project manager __uv__ (see https://docs.astral.sh/uv/getting-started/installation/)
- Download the zip from the GitHub repository (green [Code v] button at the top of the page)
- Unzip the archive and, in a terminal (Linux/macOS terminal or Windows PowerShell), go to the `Micrographs_classification-master` folder by typing the command:
`cd <path to>/Micrographs_classification-master`<br>
where `<path to>/Micrographs_classification-master` is the path to the `Micrographs_classification-master` folder, which you can retrieve using your file browser.
- Type the command `uv sync` to install the Python modules in the Python Virtual Environment of the `Micrographs_classification-master` folder.
- Launch Jupyter by typing: `uv run jupyter lab` or marimo by typing: `uv run marimo edit`.


